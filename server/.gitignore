node_modules/
dist/

# Every env file except the committed template — .env.prod holds the RDS
# password and the production JWT secret.
.env
.env.*
!.env.example

# Leftover from the JSON-file store the app used before Postgres.
data/
